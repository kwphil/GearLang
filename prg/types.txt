// This also tests a bug of crashing without a newline

// Floats
let a = 0.1 + 0.2;
let b = a + 4.0;

// Strings
let c = "Hello, World!";

fn f64 float: d f64, e char& {
    return d + 1.0;
}

return float(b, c);