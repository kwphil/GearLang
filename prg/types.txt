extern fn puts: s char& ;

// Floats
let a = 0.1 + 0.2;
let b = a + 4.0;

// Strings
let c = "Hello, World!";

fn f64 float: d f64, e char& {
    return d + 1.0;
}

// Returning pointers
fn char& str {
    return "Hello, World!";
}

// Nested function calls
puts( str( ) );

return float(b, c);