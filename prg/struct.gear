extern("C") fn printf(s char^, ...);

struct foo {
    x i16;
    y i16;
};

let s: foo;
s.x = 8200;
s.y = 40;

fn bar(s2 foo^, str char^) {
    let s3 = @s2;
    printf("%s: %d\n", str s3.x);
}

let t: i32;
t = 8;

bar(#s);

printf("in main: %d\n", s.x);
