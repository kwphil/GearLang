extern("C") fn printf(s char^, ...);

struct foo {
    x i16;
    y i16;
    z foo^;
};

fn bar(s2 foo^) {
    let s3 = @s2;
    printf("in bar: %d\n", s3.x);
}

fn main {
    let s: foo;
    s.x = 8200;

    bar(#s);

    printf("in main: %d\n", s.x);
}
